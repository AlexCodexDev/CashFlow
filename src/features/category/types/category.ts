export interface CategoryTypes {
    code: string;
    financeBookCode: string;
    name: string;
    description: string | null;
    color: string | null;
    icon: string | null;
    isActive: boolean;
}